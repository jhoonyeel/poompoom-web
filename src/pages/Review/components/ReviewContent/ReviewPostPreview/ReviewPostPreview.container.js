import ReviewPostPreviewUI from './ReviewPostPreview.presenter';

function ReviewPostPreview({ expanded, setIsExpanded }) {
  return <ReviewPostPreviewUI expanded={expanded} setIsExpanded={setIsExpanded} />;
}

export default ReviewPostPreview;
